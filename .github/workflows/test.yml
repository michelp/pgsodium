name: Run tests on all postgres versions

on: push

jobs:
  test-job:
    name: Test Job
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2        
      - name: Run test script
        run: |
          ./test.sh
